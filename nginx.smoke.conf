server {
    listen 8080;
    server_name _;

    location /health {
        add_header Content-Type text/plain;
        return 200 "ok\n";
    }

    location / {
        add_header Content-Type text/plain;
        return 200 "DebantErp smoke test - pipeline OK\n";
    }
}
