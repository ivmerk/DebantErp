@using DebantErp.Rdos
@using DebantErp.DAL.Models
@model List<UserRdo>

@{
    ViewData["Title"] = "Адміністрування — користувачі";
}

<h1 class="mb-4">Користувачі</h1>

<table class="table table-striped align-middle">
    <thead>
        <tr>
            <th>ID</th>
            <th>Email</th>
            <th>Ім'я</th>
            <th>Роль і статус</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var u in Model)
        {
            <tr>
                <td>@u.Id</td>
                <td>@u.Email</td>
                <td>@u.FirstName @u.LastName</td>
                <td>
                    <form asp-controller="Admin" asp-action="UpdateUser" asp-route-id="@u.Id" method="post"
                          class="d-flex gap-2 align-items-center">
                        <select name="role" class="form-select form-select-sm" style="width:auto">
                            @foreach (var r in Enum.GetValues<UserRoleEnum>())
                            {
                                <option value="@((int)r)" selected="@(u.Role == r)">@r</option>
                            }
                        </select>
                        <select name="status" class="form-select form-select-sm" style="width:auto">
                            @foreach (var s in Enum.GetValues<UserStatusEnum>())
                            {
                                <option value="@((int)s)" selected="@(u.Status == s)">@s</option>
                            }
                        </select>
                        <button type="submit" class="btn btn-sm btn-primary">Зберегти</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
