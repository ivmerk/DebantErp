-- «Ставка» (rate) у расценки заменена вычисляемой «стоимостью операции за штуку»
-- (дневная ставка разряда / 25500 секунд в смене × хронометраж). Ручное поле больше
-- не нужно — удаляем колонку. order_labor_costs ссылается на production_rates по id,
-- поле rate там не хранится, каскадов на rate нет.
alter table production_rates
    drop column if exists rate;
